import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "net.davidstrasak.conveyorcontroller",
  appName: "Conveyor control",
  webDir: "dist",
};

export default config;
